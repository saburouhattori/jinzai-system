# 開発・保守ガイドライン (Maintenance Guidelines)

## 引き継ぎ・保守・開発時の注意事項

* **動的マッピングの維持**:
  本システムはスプレッドシートの1行目（ヘッダー名）をキーとしてデータの読み書きを行います。列の追加・削除・入れ替えは自由に行えますが、**スクリプト内で使用しているヘッダーの文字列（例：「名前」「満年齢」など）はシート上で絶対に変更しないでください。**

* **ARRAYFORMULA等の数式保護**:
  「満年齢」や「顔写真」など、スプレッドシート上で関数によって自動出力されている列に対しては、GASからの上書き（数式の破壊）を防ぐため、「スキップ（分割書き込み）処理」が実装されています。今後、数式列を新たに追加する際は、`02_候補者管理.gs` 等の対象コード内にある `skipIndices` へ該当列を除外するロジックの追記が必要です。

* **実行時間制限（6分制限）の回避**:
  Google WorkspaceのGAS実行時間制限（6分）を回避し、パフォーマンスを保つため、配列による一括読み書き（`getValues()` / `setValues()`）を基本設計としています。シートへのアクセス（特にセルごとのループ処理）は極力避けるよう設計を維持してください。
