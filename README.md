# 人材管理システム (Jinzai System)

## 概要 (Overview)

Google Apps Script (GAS) と Google スプレッドシートを活用した人材紹介事業（Smile Dream Co.,Ltd.）向けの管理システムです。
候補者（登録者）の管理、求人案件の管理、面接から採用（内定）までのプロセスを一元管理し、業務の効率化を図ります。日本およびミャンマー拠点のスタッフが共同で運用することを想定して設計されています。

## 主な機能 (Features)

* **候補者管理**:
  「登録者マスタ」への新規登録、更新、削除。顔写真データを含んでいても高速に一括更新可能なロジックを実装。また、新規登録時には「登録日」をシステム日付で自動入力します。

* **年齢自動計算**:
  スプレッドシートの DATEDIF 関数と ARRAYFORMULA を活用し、生年月日から満年齢をリアルタイムに自動計算。メンテナンスフリーな運用を実現。

* **案件・採用管理**:
  求人案件の登録、修正、削除機能。および案件に対する候補者の「採用（内定）」登録機能。

* **面接履歴の自動追記**:
  「面接結果登録」実行時に、該当案件で面接を受けた全候補者（採用・不採用問わず）の「面接履歴」と結果を、「登録者マスタ」へ自動で改行追記します。

* **自動リスト同期 (動的マッピング)**:
  登録者マスタの更新内容を「採用者一覧」「未採用者一覧」「簡易リスト」へ自動同期。ヘッダー名（1行目の文字列）をキーにしてデータを自動振り分けするため、列の追加や移動にも柔軟に追従します。

* **直感的なUI / 待機表示**:
  データの送受信や同期など、時間のかかる非同期処理中には画面全体を覆うローディングスピナーを表示し、システムの状態を視覚的にフィードバックします。

* **ファイル・フォルダ自動連携機能（アップロード・スマートチップ化）**:
  * PCからのファイル直接アップロードに対応。自動的に「案件ID_事業者名」のフォルダを作成・特定し、ドライブへ保存してスプレッドシートと連携します。
  * 既存のGoogleドライブURL（ファイル・フォルダ）を入力・検索して追加することも可能。URLがシートに登録されると、自動的に名前を取得しファイルは「📄 Foro」、フォルダは「📁」のアイコン付きでスマートチップ風に変換します。

* **履歴書・簡易リスト生成**:
  候補者IDを指定して、提出用の履歴書や簡易リストを自動生成する機能。

* **支払い管理連携（外部連携）**:
  取引先である株式会社Funtoco様の外部シート「海外ミャンマー管理リスト【Funtoco】」へ、案件管理で確定した採用者情報をボタン一つで同期。
  同一登録者が別の案件で既に登録されている場合の重複警告機能を搭載し、Funtoco側で入力された入国日・金額等の業務データは保護しつつ、基本情報のみを安全に更新可能。転記後は自動で「案件ID順」にソートされます。

## システム構成 (System Structure)

### スプレッドシート（データベース）

* **登録者マスタ**: 全候補者のベースデータ（ID: SD-XXXX）
* **案件管理**: 求人案件データ（ID: JOB-XXXX）
* **リスト・出力用**: 未採用者一覧, 採用者一覧, 簡易リスト, 履歴書, 面接管理
* **各種マスタ**: 事業者マスタ, 送り出し機関マスタ, 日本語学校マスタ, 企業情報, 面談用質問

### Google Apps Script (バックエンド・フロントエンド)

* `01_共通・メニュー.gs` / `JS_Common.html` : 共通処理・UIの制御・メニュー構築
* `02_候補者管理.gs` / `HTML_Candidate.html` / `JS_Candidate.html` : 候補者データ操作
* `03_案件管理.gs` / `HTML_Job.html` / `JS_Job.html` : 案件データの登録・更新・削除
* `04_事業者管理.gs` / `HTML_Company.html` : 事業者データ操作
* `05_マスタ連携・その他.gs` : リストの自動同期（syncListSheets）、外部シート連携、辞書生成などの補助機能
* `06_外部連携.gs` : 取引先（Funtoco等）の外部システムとのデータ同期・転記処理
* `07_履歴書作成.gs` : 履歴書の自動出力処理
* `08_ドライブ連携.gs` : ドライブ内のファイル・フォルダ操作、ファイルアップロード処理
* `09_面接・採用管理.gs` : 面接結果の登録とマスタへの履歴追記処理
* `MainSidebar.html` / `Sidebar_CSS.html` : サイドバーのUIとスタイリング

## ファイル索引 (Index)

### GASファイル
* `01_共通・メニュー.gs` : 共通設定、共通関数、メニュー作成、各UI起動、同期実行ラッパー。入口になるファイル。
* `02_候補者管理.gs` : 候補者の新規登録、更新、削除、関連データ処理。
* `03_案件管理.gs` : 案件の追加・更新・削除、詳細取得処理。
* `04_事業者管理.gs` : 事業者マスタの登録・更新処理。
* `05_マスタ連携・その他.gs` : リスト同期、簡易リスト生成、補助関数群。
* `06_外部連携.gs` : 取引先のシステムへのデータ同期処理。
* `07_履歴書作成.gs` : 履歴書や提出用資料の生成処理。
* `08_ドライブ連携.gs` : Googleドライブ内のファイル検索・フォルダ自動作成・アップロード保存、スマートチップ変換処理。
* `09_面接・採用管理.gs` : 面接対象者の取得、面接結果（採用・不採用）の登録、全対象者の面接履歴への自動追記処理。

### HTML / UIファイル
* `MainSidebar.html` : メインUIの土台。各モードの表示切り替えに使う中心ファイル。
* `Sidebar_CSS.html` : UI共通スタイル。
* `HTML_Candidate.html` : 候補者管理画面のHTML。
* `JS_Candidate.html` : 候補者管理画面のクライアント側スクリプト。
* `HTML_Job.html` : 案件・採用管理画面のHTML。
* `JS_Job.html` : 案件・採用管理画面のクライアント側スクリプト。
* `HTML_Company.html` : 事業者管理画面のHTML。
* `JS_Common.html` : UI共通のクライアント側スクリプト。

### 読み方の目安
* 「メニューや画面の入口を見たい」→ `01_共通・メニュー.gs`
* 「候補者登録まわりを見たい」→ `02_候補者管理.gs` / `HTML_Candidate.html` / `JS_Candidate.html`
* 「案件の流れを見たい」→ `03_案件管理.gs` / `HTML_Job.html` / `JS_Job.html`
* 「面接や採用結果の処理を見たい」→ `09_面接・採用管理.gs`
* 「ファイルやフォルダの自動保存まわりを見たい」→ `08_ドライブ連携.gs`
* 「マスタや外部連携を見たい」→ `04_事業者管理.gs` / `05_マスタ連携・その他.gs` / `06_外部連携.gs`
* 「UI全体を見たい」→ `MainSidebar.html` / `Sidebar_CSS.html` / `JS_Common.html`

## 外部連携環境変数

* **Funtoco 支払い管理連携用スプレッドシートID**: `1Yo6Oz3iK6OlWjzl7BVUWeElO4__mPjJST3Jaaiys9yw`
* **案件関連ファイル保存用親フォルダID**: `1UuwRUPmldGgBR6dVUjldMI0vwZOjab0t`

## 関連ドキュメント
* [更新履歴 (CHANGELOG.md)](CHANGELOG.md)
* [今後の実装予定・課題 (TODO.md)](TODO.md)
* [開発・保守ガイドライン (MAINTENANCE.md)](MAINTENANCE.md)
