# 人材管理システム (Jinzai System)

## 概要 (Overview)
Google Apps Script (GAS) と Google スプレッドシートを活用した人材紹介事業向けの管理システムです。
候補者（登録者）の管理、求人案件の管理、面接から採用（内定）までのプロセスを一元管理し、業務の効率化を図ります。日本およびミャンマー拠点のスタッフが共同で運用することを想定して設計されています。

## 主な機能 (Features)
* **候補者管理**: 「登録者マスタ」への新規登録、更新、削除。顔写真データを含んでいても高速に一括更新可能なロジックを実装。
* **年齢自動計算**: スプレッドシートの `DATEDIF` 関数と `ARRAYFORMULA` を活用し、生年月日から満年齢をリアルタイムに自動計算。メンテナンスフリーな運用を実現。
* **案件・採用管理**: 求人案件の登録、修正、削除機能。および案件に対する候補者の「採用（内定）」登録機能。
* **自動リスト同期**: 候補者のステータス（未採用・採用）に応じて、該当シート（「未採用者一覧」「採用者一覧」）へデータを自動振り分け・同期。特定技能要件の抽出と表記統一（例: 外食業技能（合格）など）も自動化。
* **ファイルリンク連携**: 関連ファイルURLをGoogleドライブのファイル名を取得して「スマートチップ風」に自動変換。
* **履歴書・簡易リスト生成**: 候補者IDを指定して、提出用の履歴書や簡易リストを自動生成する機能。

## システム構成 (System Structure)
### スプレッドシート（データベース）
* **登録者マスタ**: 全候補者のベースデータ（ID: SD-XXXX）
* **案件管理**: 求人案件データ（ID: JOB-XXXX）
* **リスト・出力用**: 未採用者一覧, 採用者一覧, 簡易リスト, 履歴書, 面接管理
* **各種マスタ**: 事業者マスタ, 送り出し機関マスタ, 日本語学校マスタ, 企業情報, 面談用質問

### Google Apps Script (バックエンド・フロントエンド)
* `01_共通・メニュー.gs` / `JS_Common.html` : 共通処理・UIの制御
* `02_候補者管理.gs` / `HTML_Candidate.html` / `JS_Candidate.html` : 候補者データ操作
* `03_案件・採用管理.gs` / `HTML_Job.html` / `JS_Job.html` : 案件データ操作・採用登録
* `04_事業者管理.gs` / `HTML_Company.html` : 事業者データ操作
* `05_マスタ連携・その他.gs` : リストの自動同期（`syncListSheets`）、辞書生成などの補助機能
* `履歴書作成.gs` : 履歴書の自動出力処理
* `MainSidebar.html` / `Sidebar_CSS.html` : サイドバーのUIとスタイリング

---

## 更新履歴 (Changelog)
* **[2026/04/09] 年齢管理の仕様変更とエラー解消**
    * GASによる年齢更新（`updateAllAges`）を廃止し、スプレッドシートの関数（`ARRAYFORMULA` + `DATEDIF`）による自動計算へ移行。スクリプト実行エラーを解消し、メンテナンス性を向上。
* **[2026/04/04] 未採用者一覧の拡張**
    * E列に「特定技能要件」を新設。JLPT、JFT、介護、外食業、飲食料品製造業などの資格・試験ステータスをマスタから自動同期。
* **[2026/04/04] 案件管理シートの最適化**
    * 案件登録時、テーブルの最終行を自動判定して行を挿入する仕様に変更。日付データを `yyyy-MM-dd` 形式で安定化。
* **[2026/04/04] システム安定性の向上**
    * UIのフリーズ不具合を解消。非同期通信に `withFailureHandler` と `try-catch` を実装。

---

## 今後の実装予定・課題 (TODO)
* [ ] **スプレッドシートの入力ルール策定・入力規則の設定**
    * 全角・半角の混在や表記揺れを防ぐため、「データの入力規則」を設ける。
* [ ] **採用者登録時のバリデーション（入力チェック）強化**
    * 存在しない候補者IDが入力された際に、エラーとして弾く安全装置を実装。
* [ ] **フォームの多言語対応（自動翻訳切り替え）**
    * 使用者の言語設定に応じて、UIの表示言語を自動で切り替える機能（※現地スタッフと翻訳調整後に着手）。
     
    * jinzai-systemGoogle Apps Script (GAS) を用いた人材事業向けの統合管理システムです。スプレッドシートをデータベースとして活用し、サイドバーUIから候補者情報、案件情報、採用情報の登録・更新・削除をスムーズに行うことができます。主な機能候補者管理: 候補者の新規登録、情報更新、採用情報の追加、および削除。事業者・案件管理: 事業者のマスタ登録、案件の新規登録、更新、削除。採用・面接管理: 案件に対する採用者の登録とステータス管理。リストの動的同期機能: 登録者マスタの更新内容を「採用者一覧」「未採用者一覧」「簡易リスト」へ自動同期。（ヘッダー名による動的マッピングにより、列の追加や移動にも柔軟に追従します）履歴書・リスト出力: 登録データに基づいた履歴書の自動生成や簡易リストの出力。アーキテクチャバックエンド: Google Apps Script (GAS)フロントエンド: HTML, CSS, Vanilla JavaScriptデータベース: Google Spreadsheetセットアップ手順本リポジトリのコードを Google Apps Script プロジェクトにインポートします。01_共通・メニュー.gs の onOpen 関数が実行されると、スプレッドシートに専用メニューが追加されます。スプレッドシートの「人材事業メニュー」や「案件・採用管理」からサイドバーを開いてシステムを操作します。システムの堅牢性に関する仕様（列の変更について）本システムの一覧出力・同期機能は、**「ヘッダーの項目名（1行目の文字列）」をキーにしてデータを自動的に振り分ける仕組み（動的マッピング）**を採用しています。そのため、以下の操作を安全に行うことができます。各種一覧シートへの新しい列の追加や、既存列の入れ替え（コードの修正は不要です）※ただし、1行目の「ヘッダー名」そのものを変更した場合や、必須項目（登録者IDなど）を削除した場合は連携エラーの原因となりますのでご注意ください。
